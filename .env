VITE_MOCK_MODE=true
# Comma-separated list of wss HotPocket server URLs (used when VITE_MOCK_MODE=false)
VITE_CONTRACT_URLS=wss://localhost:8081
# Protocol must be 'json' for this backend (it parses JSON only)
VITE_HP_PROTOCOL=json
